[package]
name = "skipper-preload"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
nix = { workspace = true }
serde_json = { workspace = true }
regex = { workspace = true }
